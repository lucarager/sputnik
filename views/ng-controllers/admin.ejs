        <div ng-controller="AdminController" class="container">
          <div class="col-md-8 col-md-offset-2">
            <div class="form-group">
              <label>Subject</label>
              <input type="subject" class="form-control" placeholder="Subject">
            </div>
            <div class="form-group">
              <label>Channel</label>
              <select class="form-control" name="channel">
            
              </select>
            </div>
            <div class="form-group">
              <label>Send At</label>
              <input type="send_at" class="form-control" placeholder="YYYY-MM-DD HH:MM:SS">
              <div class="text-block">YYYY-MM-DD HH:MM:SS for when to send the email. Note that this does cost more, and requires a positive balance on your account. At least that's what the docs say.</div>
            </div>
            <div class="form-group">
              <label>Subject</label>
              <textarea type="text" class="form-control" placeholder="Your Message" rows="10"></textarea>
            </div>
            <div class="form-group">
              <label>Passkey</label>
              <input type="subject" class="form-control" placeholder="Your Passkey">
            </div>
          </div>
          <div class="col-md-4 col-md-offset-4">
            <button class="btn btn-primary btn-block btn-lg" ng-click="save()" ng-disabled="!login.logged || save_text[0]">
              <span class="ion-looping" ng-if="save_text[0]"></span>
              Save
            </button>
          </div>
        </div>